# 复制为同目录 .env 后由 docker compose 加载（已在根 .gitignore 忽略 infra/litellm/.env）

# 与 Next.js apps/web/.env.local 里的 LITELLM_API_KEY 保持一致（客户端调 Proxy 的 Bearer）
LITELLM_MASTER_KEY=sk-canonweave-litellm-dev

# 各上游厂商 Key（按你在 config.yaml 里启用的模型填写）
DEEPSEEK_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
